{
  "name": "galeria-fotos",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "gen:placeholders": "node scripts/gen-placeholders.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "sharp": "^0.33.0",
    "vitest": "^2.0.0"
  }
}
